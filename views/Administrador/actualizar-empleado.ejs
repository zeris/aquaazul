<!DOCTYPE html>
<html>
    <head>
        <title>Aqua azul - Actualizar Empleado</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
        <script type="text/javascript" src="/js/formValidator.js"></script>
        <%- include('./partials/head'); %>
    </head>
    <body>
        <%- include('./partials/navbar'); %>
        <div class="row all-area">
         <div class="col-lg-3 col-md-12 col-12">
            <%- include('./partials/menu-empleados'); %>
         </div>
         <div class="col-lg-9 col-md-12 col-12">
            <div class="content container">
               <% if(error) { %>
                  <div class="alert alert-danger">
                      <button class="close" data-dismiss="alert"><span>&times;</span></button>
                      <%= mensajeError %>
                  </div>
              <% } %>
               <div class="row justify-content-center top-margin-3">
                  <h3 class="text-center">Ingrese los datos a modificar del empleado</h3>
               </div>
               <div class="row justify-content-center top-margin-5">
                  <form action="/administrador/empleados/actualizar/<%= empleado.ID_USUARIO %>" method="POST"  onsubmit="return verificarInput();">
                     <div class="form-row padding-width-400px">
                        <div class="col-12">
                           <h5>Nombre</h5>
                           <input class="form-control" type="text" name="nombre" value="<%= empleado.NOMBRE %>" onkeyup="validateNames(event)" required/>
                           <div id="message-nombre" class="invalid-feedback" hidden>
                              Introduzca solo letras (A-Z) o (a-z). Máximo 50 caracteres.
                           </div>
                        </div>
                   
                        <div class="col-12">
                           <h5>Apellido Paterno</h5>
                           <input class="form-control" type="text" name="apellidoPaterno" value="<%= empleado.APELLIDO_P %>" onkeyup="validateNames(event)" required/>
                           <div id="message-apellidoPaterno" class="invalid-feedback" hidden>
                              Introduzca solo letras (A-Z) o (a-z). Máximo 50 caracteres.
                           </div>
                        </div>
                     
                        <div class="col-12">
                           <h5>Apellido Materno</h5>
                           <input class="form-control" type="text" name="apellidoMaterno" value="<%= empleado.APELLIDO_M %>" onkeyup="validateNames(event)" required/>
                           <div id="message-apellidoMaterno" class="invalid-feedback" hidden>
                              Introduzca solo letras (A-Z) o (a-z). Máximo 50 caracteres.
                           </div>
                        </div>
                     
                        <div class="col-12">
                           <h5>Email</h5>
                           <input class="form-control" type="email" name="email" value="<%= empleado.EMAIL%>" onkeyup="validateNames(event)" required/>
                           <div id="message-email" class="invalid-feedback" hidden>
                              Correo no valido. Máximo 60 caracteres.
                           </div>
                        </div>
                      
                        <div class="col-12 justify-content-center d-flex top-margin-5">
                           <input class="btn btn-primary col-12" type="submit" value="Actualizar Empleado"/>
                        </div>
                     </div>          
                 </form>
               </div>
            </div>
         </div>
    </body>
</html>